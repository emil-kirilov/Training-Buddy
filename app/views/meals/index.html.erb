<%= flash[:error] if flash[:error] %>
<%= flash[:notice] if flash[:notice] %>

<table>
	<tr>
		<th>food</th>
		<th>grams</th>
		<th colspan="2">Options</th>
	</tr>

	<tbody>
    <% if @meals.empty? %>
      <strong> No meals yet</strong>
    <% else %>
    	<% @meals.each do |meals| %>
    		<tr>
    			<td><%= meals.food_id %></td>
    			<td><%= meals.grams %></td>
                <td><%= link_to 'View meals', day_meals_path(params[:day_id], meals.id) %></td>
                <td><%= link_to 'Delete meals', meals,  method: :delete, data: { confirm: 'Are you sure?' } %></td>
    		</tr>
    	<% end %>
		<% end %>
	</tbody>
</table>
 